#include <iostream>
#include <cstdlib>
#include <ctime>
#include <random>
#include <vector>

using namespace std;

int main()
{
    srand(time(0));

    cout<<a;
    vector<double> Sequence;

    for(int i=0;i<14 400; i++)
    {
        double a = (double)rand()/RAND_MAX;
        Sequence.at(a);
    }
    return 0;
}
